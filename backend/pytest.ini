[pytest]
addopts = --ignore=nul
norecursedirs = .git .hg .svn build dist nul
testpaths = tests
asyncio_mode = auto
markers =
    flaky_quarantine: temporarily quarantined flaky tests; requires issue=<ticket>, owner=<team-or-user>, expires=YYYY-MM-DD
