[pytest]
addopts = --ignore=nul
norecursedirs = .git .hg .svn build dist nul
testpaths = tests
asyncio_mode = auto
markers =
    flaky_quarantine: temporarily quarantined flaky tests (requires issue/owner and removal date)
